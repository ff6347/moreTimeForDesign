main();function main(){        var doc = app.documents.add();    doc.documentPreferences.facingPages =false;        var allImages = loadFiles ();            for(var i =0;i < allImages.length;i++){                 var pg = doc.pages.add();         try{         var rect = pg.rectangles.add({             geometricBounds:[             -3,             -3,             doc.documentPreferences.pageHeight + 3,             doc.documentPreferences.pageWidth + 3 ]             });                 rect.place(allImages[i]);        rect.fit(FitOptions.PROPORTIONALLY);        rect.fit(FitOptions.CENTER_CONTENT);        }catch(e){             pg.remove();                        };        };        };function loadFiles(){var theSelectedFolder = Folder.selectDialog ("Choose the Folder");    if(!theSelectedFolder ){        return;        };        allImages = theSelectedFolder.getFiles ("*.*");        return allImages;        };