{var counter = 0;var numberOfDocuments = 22;while(counter <= numberOfDocuments){app.documents.add({    documentPreferences:{        pageWidth:100,pageHeight:100        }    });var randomNumber = Math.floor( Math.random()*app.fonts.length-1 );var tf = app.activeDocument.pages.item(0).textFrames.add({    geometricBounds:[10,10,90,90],    contents:String(counter) + "\nHello World in\n" + app.fonts.item(randomNumber).name    });tf.characters.everyItem().properties = {    pointSize: 23,    appliedFont: app.fonts.item(randomNumber)     };counter++;}}